#include <stdio.h>
int main() {
    int num = 25;   // variable declaration and initialization
    printf("Value of variable num = %d\n", num);
    printf("Address of variable num = %p\n", &num);
    return 0;
}
